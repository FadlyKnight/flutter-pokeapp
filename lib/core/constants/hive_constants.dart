/// Names of the Hive boxes used across the app.
class HiveBoxes {
  const HiveBoxes._();

  static const String favorites = 'favorites_box';
  static const String settings = 'settings_box';
  static const String pokemonListCache = 'pokemon_list_cache_box';
  static const String pokemonDetailCache = 'pokemon_detail_cache_box';
}

/// Keys used inside the settings box.
class SettingsKeys {
  const SettingsKeys._();

  static const String themeMode = 'theme_mode';
}
